#include<stdio.h>
// swap tset
int main(void) 
{
	/*
		int a, b;
		int temp;

		scanf("%d %d", &a, &b);

		temp = a;
		a = b;
		b = temp;

		printf("%d %d", a, b);
	*/

	int a, b;
	scanf("%d %d", &a, &b);
	printf("%d %d", b, a);
	return 0;
}